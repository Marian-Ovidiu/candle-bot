{
  "name": "candle-bot-5m",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "clean": "rimraf dist",
    "test": "vitest run",
    "test:watch": "vitest",
    "backtest": "npm run build && node -r dotenv/config dist/backtest/runBacktest.js",
    "backtest:dev": "tsx -r dotenv/config src/backtest/runBacktest.ts",
    "convert": "tsx convertSpikeData.ts",
    "collect": "tsx data/collect/coinbaseCollector.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "dotenv": "^17.4.2"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "rimraf": "^6.1.3",
    "tsx": "^4.21.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.5"
  }
}
